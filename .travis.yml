language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-knockout-binding' ]; then cd .. && eval "mv $currentfolder generator-knockout-binding" && cd generator-knockout-binding; fi

